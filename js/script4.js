#!/c/PROGRA~1/nodejs/node
let fatal = "For zombies I've concluded that we have three fatal mistakes"
for (let x = 0; x < fatal.length; x++) {
    console.log(fatal[x])
}

// simple for loop
for (let i = 0; i < 20; i++) {
    console.log(i)
}